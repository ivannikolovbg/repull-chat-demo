# Required — server-side only, never exposed to the browser
ANTHROPIC_API_KEY=sk-ant-...

# Optional — enables Sandbox toggle (server-controlled demo workspace key)
REPULL_DEMO_API_KEY=

# Override Repull API base (default: https://api.repull.dev)
# REPULL_API_BASE_URL=https://api.repull.dev
